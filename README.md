# FBX Normalize Bones
Adds a toggle in Export FBX's Armature tab to Export your Rig with normalized bones. You need to have the bones inside a Bone Collection named `Normalize` or `NormalizeBones`.

To Install just drop it into your Addon/Scripts folder.
